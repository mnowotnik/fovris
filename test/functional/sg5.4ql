module sg:
    relations:
        flat(literal,literal).
        up(literal,literal).
        sg(literal,literal).
        down(literal,literal).
    rules:
        sg(X,Y) :- flat(X,Y).
        sg(X,Y) :- up(X,X1), sg(X1,Y1), down(Y1,Y).
    facts:
        up(a,b0).
        up(a,b1).
        up(a,b2).
        up(a,b3).
        up(a,b4).
        up(b0,c0).
        up(b0,c1).
        up(b0,c2).
        up(b0,c3).
        up(b0,c4).
        up(b1,c0).
        up(b1,c1).
        up(b1,c2).
        up(b1,c3).
        up(b1,c4).
        up(b2,c0).
        up(b2,c1).
        up(b2,c2).
        up(b2,c3).
        up(b2,c4).
        up(b3,c0).
        up(b3,c1).
        up(b3,c2).
        up(b3,c3).
        up(b3,c4).
        up(b4,c0).
        up(b4,c1).
        up(b4,c2).
        up(b4,c3).
        up(b4,c4).
        flat(c0,d0).
        flat(c0,d1).
        flat(c0,d2).
        flat(c0,d3).
        flat(c0,d4).
        flat(c1,d0).
        flat(c1,d1).
        flat(c1,d2).
        flat(c1,d3).
        flat(c1,d4).
        flat(c2,d0).
        flat(c2,d1).
        flat(c2,d2).
        flat(c2,d3).
        flat(c2,d4).
        flat(c3,d0).
        flat(c3,d1).
        flat(c3,d2).
        flat(c3,d3).
        flat(c3,d4).
        flat(c4,d0).
        flat(c4,d1).
        flat(c4,d2).
        flat(c4,d3).
        flat(c4,d4).
        down(d0,e0).
        down(d0,e1).
        down(d0,e2).
        down(d0,e3).
        down(d0,e4).
        down(d1,e0).
        down(d1,e1).
        down(d1,e2).
        down(d1,e3).
        down(d1,e4).
        down(d2,e0).
        down(d2,e1).
        down(d2,e2).
        down(d2,e3).
        down(d2,e4).
        down(d3,e0).
        down(d3,e1).
        down(d3,e2).
        down(d3,e3).
        down(d3,e4).
        down(d4,e0).
        down(d4,e1).
        down(d4,e2).
        down(d4,e3).
        down(d4,e4).
        down(e0,f).
        down(e1,f).
        down(e2,f).
        down(e3,f).
        down(e4,f).
end.
sg.sg(X,Y)?
